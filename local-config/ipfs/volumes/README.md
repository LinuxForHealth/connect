# IPFS Volumes

## Overview
connect/local-config/ipfs/volumes contains the mount points for the volumes mounted to the IPFS cluster and node services.  For a fresh restart of the IPFS cluster node, stop the connect services, delete the directories in connect/local-config/ipfs/volumes and restart the connect services.
